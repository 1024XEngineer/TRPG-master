# 复制成 .env 并填入真实值，.env 本身不提交 git。
#
# 数据库跑在共用服务器上的 Docker 容器里（只绑 127.0.0.1，不对公网暴露），
# 本地开发前先起一条 SSH 隧道（把服务器的 5432 转发到本地 5433）：
#   ssh -p <SSH端口> -N -L 5433:127.0.0.1:5432 <你的服务器账号>@<服务器地址>
# 找项目负责人要服务器地址/端口/SSH 账号密码或密钥，以及下面这个 DATABASE_URL 里的数据库密码。
DATABASE_URL=postgresql+asyncpg://aidm:<数据库密码，找项目负责人要>@localhost:5433/aidm

# 国内大模型，先全部角色共用 DeepSeek（兼容 OpenAI SDK），见 core/ai/config.py
# 去 https://platform.deepseek.com 申请自己的 key，或找项目负责人要共用 key。
DEEPSEEK_API_KEY=<你的 DeepSeek API Key>
