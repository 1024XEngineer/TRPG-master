node_modules/
dist/
.vite/
*.tsbuildinfo
.DS_Store

.venv/
__pycache__/
.pytest_cache/
.ruff_cache/
*.db

# scripts/export_schema.py 的输出：Python -> TS 类型生成管线的临时中间产物，
# 不是需要跨机器/跨提交保持一致的东西（真正提交的是 trpg-sdk/src/generated/
# 下由它生成出来的 TS 类型），见 issue #75 决策 3b。
.schema-export/

# Playwright 的运行产物：报告、trace、失败截图/视频。CI 上是作为 artifact
# 上传的（见 .github/workflows/e2e-ci.yml），不进 git。
e2e/playwright-report/
e2e/test-results/

.env
.env.*
!.env.example
