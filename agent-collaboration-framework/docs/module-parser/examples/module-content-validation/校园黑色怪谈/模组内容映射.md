# 校园黑色怪谈 → ModuleContent v1 内容映射

> 状态：Contract Validation
> 锚定：`module-content-field-decisions.md` §8 冻结字段
> 模组特征：非线性高中调查、奈亚拉托提普教团、七大怪谈、多结局

---

## ModuleIdentity

| 原模组内容 | 映射字段 |
|-----------|---------|
| "校园黑色怪谈 -Things Uncanny in BLACK Campus-" | `module_id="black-campus"` |
| CoC 7e | `world_ref="coc-7e"` |
| 3-6 人，现代中国 | `version="0.1.0"` |

## ModuleFrame / 背景

| 原模组内容 | 映射字段 |
|-----------|---------|
| 传教士建造学校→奈亚拉托提普信徒→饲养祭品 | `Entity(missionary).secrets` + `Entity(principal_shi).secrets` |
| "大多数教师都或多或少接受了奈亚拉托提普的诱惑" | `Entity.teachers.state(corrupted)` + `Entity.secrets` |
| 隐修堂调查员被杀→尸体埋花坛 | `Scene(garden).content` + `Entity(garden).secrets` |
| 七个校园怪谈的背景 | 各 `Scene.content` 自然语言承载 |

## Scene

| 原模组内容 | 映射 |
|-----------|------|
| 教学楼 1（5 层） | `Scene(teaching_building_1)`。entity_ids=[classrooms, stairway_13]。checkpoint_ids 包含各怪谈触发点 |
| 教学楼 2 | `Scene(teaching_building_2)`。entity_ids=[music_room, art_room, model_room] |
| 花园（围挡） | `Scene(garden)`。checkpoint_ids=[search_garden, dig_flowerbed]。visibility(requires_discovery=true) |
| 地下密室 | `Scene(underground_chamber)`。checkpoint_ids=[investigate_chamber, confront_cult] |
| 校长办公室 | `Scene(principal_office)`。entity_ids=[principal_shi, mythos_books] |
| 朱莉办公室 | `Scene(julie_office)`。entity_ids=[julie, ancient_manuscripts] |
| 操场/体育馆 | `Scene(playground)` |
| 图书馆 | `Scene(library)`。checkpoint_ids=[research_history, find_busen_notebook] |

非线性：`exits=[]`（校园内自由移动）。

## Entity

| 原模组内容 | 映射 |
|-----------|------|
| 史校长 | `Entity(principal_shi, kind=npc)`。state={corrupted:true}。secrets="奈亚拉托提普信徒，主持献祭" |
| 朱莉（副校长） | `Entity(julie, kind=npc)`。state={corrupted:true}。secrets="比史校长更狂热，管理地下密室" |
| 王贵强（学区主任） | `Entity(wang_guiqiang, kind=npc)`。state={suspicious:true}。content="正直的学区主任" |
| "隐修堂"调查员（布文杉） | `Entity(buwen_shan, kind=npc)`。state={alive:false}。secrets 包含其身份和死因 |
| 传教士 | `Entity(missionary, kind=npc)`。历史人物，自然语言承载 |
| 奈亚拉托提普 | 外神。`Entity.secrets` 自然语言——不建 StatBlock |
| 教师群体 | `Entity(teachers, kind=npc)`。state={corrupted:true/false 逐人} |
| 布森的笔记本（神话典籍） | `Entity(busen_notebook, kind=object)`。state={found:false, read:false} |
| 布文杉的枪 | `Entity(buwen_gun, kind=object)`。state={found:false} |
| 地下密室祭坛 | `Entity(altar, kind=object)`。state={active:true} |

## Checkpoint

| 原模组内容 | 映射 |
|-----------|------|
| 十三阶梯（怪谈 1） | `Checkpoint(count_stairs)`。skills=["spot-hidden"]。visibility(requires_discovery=true) |
| 理化教室女学生（怪谈 2） | `Checkpoint(investigate_science_room)` |
| 会走路的人体模型（怪谈 3） | `Checkpoint(investigate_model_room)` |
| 音乐教室回音（怪谈 4） | `Checkpoint(investigate_music_room)` |
| 调查花园 | `Checkpoint(search_garden)`。success→发现领带（"森"字） |
| 挖掘花坛 | `Checkpoint(dig_flowerbed)`。success→发现布文杉尸体 |
| 暗骰：保安巡逻 | `Checkpoint(evade_guard)`。visibility(audience="keeper", requires_discovery=false) |
| 阅读布森笔记本 | `Checkpoint(read_busen_notebook)`。SAN loss 由 Ruleset 解释 |
| 对抗史校长 | `Checkpoint(confront_principal)`。战斗相关 |
| 解除祭坛 | `Checkpoint(deactivate_altar)` |

## Rule

| 原模组内容 | 映射 |
|-----------|------|
| 教师蛊惑状态影响对话 | `Rule(on_interact, when teacher.corrupted==true, then=[ModifyOp(check.difficulty, "hard")])` |
| SAN：阅读神话典籍 1d6+1 | `Rule(on_read("busen_notebook"), then=[ModifyOp(actor.sanity, "-1d6-1")])` — Ruleset 解释骰子 |
| SAN：目击奈亚拉托提普显现 | `Rule(on_scene_enter("underground_chamber"), when altar.active==true, then=[ModifyOp(actor.sanity, "-1d10")])` |
| 暗骰机制 | `Checkpoint.visibility(audience="keeper")` — 玩家不感知检定 |

## WinCondition

| 原模组内容 | 映射 |
|-----------|------|
| 摧毁祭坛→成功 | `WinCondition(altar_destroyed, when altar.active==false, is_ending=true)` |
| 调查员被献祭→失败 | `WinCondition(sacrificed, when actor.captured==true, is_ending=true)` |
| 调查员退缩（附录 i 多种结局） | 多个 `WinCondition`，各自条件 |

## Visibility

| 原模组内容 | 映射 |
|-----------|------|
| 暗骰（多处） | `Checkpoint.visibility(audience="keeper")` |
| 地下密室未发现 | `Scene(underground_chamber).checkpoint_ids[...].visibility(requires_discovery=true)` |
| 教师秘密 | `Entity.secrets` 字段级隔离 |
| 传教士历史真相 | `Entity(missionary).secrets` — Keeper-only |

## Gap 记录

| # | 内容 | 分类 | 说明 |
|---|------|------|------|
| 1 | 七怪谈的"发现条件"多为环境触发，非单个检定 | Parser Gap | Parser 需从自然语言中识别"这里有一个暗骰"。Contract 层面 `visibility(audience="keeper")` 已可表达 |
| 2 | 教师群体蛊惑状态（"或多或少受到诱惑"） | Host Agent Gap | "或多或少"是模糊语义。A 的 IntentParser 在扮演教师时参考此状态，但不执行确定性规则 |
| 3 | SAN（多处神话接触） | Ruleset Gap | 数值变化由 `Rule + ModifyOp` 表达。骰子公式由 CoC Ruleset 解释 |
| 4 | "KP 可以自行创造合理的结局" | Host Agent Gap | 附录 i 列举的结局可映射为 WinCondition。KP 自创的不在 Contract 处理范围 |
| 5 | 奈亚拉托提普（外神）无 StatBlock | Future Capability | 外神/神话存在的属性属于 Ruleset Reference，不在模组 Content 中声明 |
