# 追书人 → ModuleContent v1 内容映射

> 状态：Contract Validation
> 锚定：`module-content-field-decisions.md` §8 冻结字段

---

## ModuleIdentity

| 原模组内容 | 映射字段 |
|-----------|---------|
| 标题 "追书人"（Paper Chase） | `module_id="paper-chase-zh-coc7"` |
| CoC 7e 规则 | `world_ref="coc-7e"` |
| 单人模组 | `version="0.1.0"`（内容版本） |

## ModuleFrame / 背景

| 原模组内容 | 映射字段 |
|-----------|---------|
| 道格拉斯背景（"跟随食尸鬼进入地下世界"） | `Entity(douglas).secrets` |
| 玩家信息（"托马斯委托调查失窃和失踪"） | `Scene(briefing).content` |
| 主持指导（"调查员可自由选择调查路径"） | `Scene.content` 内自然语言承载 |

## Scene

| 原模组内容 | 映射 |
|-----------|------|
| 书房委托 | `Scene(client_briefing)`。entity_ids=[thomas]，无 Checkpoint |
| 询问邻居 | `Scene(neighborhood)`。entity_ids=[lyla]，checkpoint_ids=[social_neighbors] |
| 图书馆 | `Scene(library)`。checkpoint_ids=[library_research] |
| 报社 | `Scene(newspaper)`。entity_ids=[hilda]，checkpoint_ids=[access_archive, luck_find] |
| 金博尔宅 | `Scene(kimball_house)`。entity_ids=[window, diary]，checkpoint_ids=[search_study, read_diary] |
| 墓地 | `Scene(cemetery)`。checkpoint_ids=[track_at_grave] |
| 地穴 | `Scene(crypt)`。checkpoint_ids=[move_crypt_slab]。visibility.requires_discovery=true |
| 对峙/对话 | `Scene(confrontation)`。entity_ids=[douglas] |

自由移动：`exits=[]`（无空间约束）。

## Entity

| 原模组内容 | 映射 |
|-----------|------|
| 托马斯 | `Entity(thomas, kind=npc)`。content="神色谨慎的老管家"。secrets="知道柜中藏文件" |
| 道格拉斯 | `Entity(douglas, kind=npc)`。content="失踪一年的爱书人"。secrets="已成为食尸鬼"。stat_block={STR:85,CON:75,DEX:40,HP:14,armor:"火器减半"} |
| 莱拉（邻居） | `Entity(lyla, kind=npc)`。无 stat_block |
| 看守 | `Entity(melodias, kind=npc)`。state={cooperative:false} |
| 书架 | `Entity(bookshelf, kind=object)`。state={key_found:false} |
| 柜子 | `Entity(cabinet, kind=object)`。refuse_ops=["open"]。blocked_text="柜门纹丝不动" |
| 文件 | `Entity(document, kind=object)`。state={obtained:false, destroyed:false} |
| 酒 | `Entity(liquor, kind=object)`。state={obtained:false} |
| 日记 | `Entity(diary, kind=object)`。state={found:false, read:false} |

## Checkpoint

| 原模组内容 | 映射 |
|-----------|------|
| 调查书架 | `Checkpoint(investigate_bookshelf)`。skills=["spot-hidden"]。success→modify key_found=true |
| 砸柜子（C类反转） | `Checkpoint(smash_cabinet)`。skills=["STR"]。success→modify opened=true + destroyed=true（坏结果） |
| 交涉邻居 | `Checkpoint(social_neighbors)`。skills=["charm","fast-talk"] |
| 图书馆研究 | `Checkpoint(library_research)`。skills=["library-use"] |
| 报社查阅 | `Checkpoint(access_newspaper_archive)` |
| 追踪足迹→地穴 | `Checkpoint(track_at_grave)`。visibility.requires_discovery=true |
| 搬开地穴石板 | `Checkpoint(move_crypt_slab)`。skills=["STR"] |
| 贿赂看守 | `Checkpoint(bribe_caretaker)`。Condition: entities.liquor.obtained==true |

## Rule

| 原模组内容 | 映射 |
|-----------|------|
| 有钥匙才能开柜子 | `Rule(on_interact, when key_found==true, then=[allow("open"), modify(opened=true), modify(obtained=true)])` |
| SAN：目击道格拉斯 0/1D6 | `Rule(on_scene_enter("confrontation"), when douglas.sighted==true, then=[ModifyOp(actors.*.sanity, -1d6)])` — Ruleset 负责解释骰子表达式 |
| 累计封顶 | `Rule(on_state_change, when sanity_loss_from_ghoul>=6, mode=forbid)` |

## WinCondition

| 原模组内容 | 映射 |
|-----------|------|
| 获得文件→正常结局 | `WinCondition(ending_document_recovered, when document.obtained==true, is_ending=true)` |
| 文件损毁→坏结局 | `WinCondition(ending_document_destroyed, when document.destroyed==true, is_ending=true)` |

## Visibility

| 原模组内容 | 映射 |
|-----------|------|
| 地穴入口需追踪成功才发现 | `Checkpoint(track_at_grave).visibility(requires_discovery=true, discovery_rule="check.track.success==true")` |
| 道格拉斯 secrets 永远不可见 | `Entity.secrets` 字段级隔离——不进 PlayerView |

## Gap 记录

| # | 内容 | 分类 | 说明 |
|---|------|------|------|
| 1 | 夜间监视循环（"每晚监视直到人影出现"） | Runtime Gap | Contract 可声明 on_time_elapsed Hook。B 的 Scheduler 未实现 |
| 2 | "礼貌询问"等开放式行动 | Host Agent Gap | 无封闭 Outcome 集合。A 的 Narrator 自然语言处理 |
| 3 | SAN loss 骰子表达式 | Ruleset Gap | "0/1d6" 由 CoC Ruleset 解释。模组只声明数值变化 |
