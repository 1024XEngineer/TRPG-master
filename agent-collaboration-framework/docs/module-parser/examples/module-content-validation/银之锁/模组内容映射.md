# 银之锁 → ModuleContent v1 内容映射

> 状态：Contract Validation
> 锚定：`module-content-field-decisions.md` §8 冻结字段

---

## ModuleIdentity

| 原模组内容 | 映射字段 |
|-----------|---------|
| 标题 "银之锁" | `module_id="silver-lock"` |
| 当代背景，CoC 7e | `world_ref="coc-7e"` |

## ModuleFrame / 背景

| 原模组内容 | 映射字段 |
|-----------|---------|
| "你在昏暗的房间中醒来，失去记忆，手脚被捆住" | `Scene(room_awakening).content` |
| 银之锁法术原理 | `Entity(silver_lock).secrets` |
| 芭斯特（猫）的真相 | `Entity(cat).secrets` |
| 人物卡定制（"依照人物的重要之物写模组"） | 不可结构化——D 类。编译期个性化 |

## Scene

| 原模组内容 | 映射 |
|-----------|------|
| 床段 | `Scene(bed_segment)`。entity_ids=[bed, rope]。checkpoint_ids=[move_bed, untie_rope] |
| 书桌段 | `Scene(desk_segment)`。entity_ids=[desk, drawer, note, sketchbook]。checkpoint_ids=[open_drawer, read_note, tear_sketch] |
| 衣柜段 | `Scene(wardrobe_segment)`。entity_ids=[wardrobe, white_paper]。checkpoint_ids=[open_wardrobe, take_paper] |
| 通风管段 | `Scene(vent_segment)`。checkpoint_ids=[enter_vent] |
| 房门段 | `Scene(door_segment)`。checkpoint_ids=[unlock_door, open_door] |
| 走廊 | `Scene(corridor)`。entity_ids=[cat, kidnapper]。checkpoint_ids=[rescue_cat, feed_cat, confront_kidnapper, escape] |

空间拓扑：`exits` 逐 Scene 声明。房间→走廊有约束。

## Entity

| 原模组内容 | 映射 |
|-----------|------|
| 猫（芭斯特） | `Entity(cat, kind=npc)`。state={alive:true, rescued:false}。secrets="猫是破解银之锁的关键" |
| 人面鼠 | `Entity(rat_thing, kind=npc)`。stat_block={DEX:60, HP:8} |
| 绑架者 | `Entity(kidnapper, kind=npc)`。stat_block={STR:50, HP:12} |
| 速写本 | `Entity(sketchbook, kind=object)`。state={used:false} |
| 钥匙（多把） | `Entity(keys, kind=object)`。state={desk_key:false, door_key:false} |
| 字条 | `Entity(note, kind=object)`。state={read:false}。content 承载字条内容 |
| 白纸 | `Entity(white_paper, kind=object)` |
| 抽屉 | `Entity(drawer, kind=object)`。state={opened:false} |

## Checkpoint

| 原模组内容 | 映射 |
|-----------|------|
| 移动床 | `Checkpoint(move_bed)`。skills=["STR"] |
| 开抽屉 | `Checkpoint(open_drawer)`。skills=["locksmith"] |
| 读字条 | `Checkpoint(read_note)`。无检定，direct |
| 撕速写 | `Checkpoint(tear_sketch)`。无检定 |
| 画物品 | `Checkpoint(draw_item)`。**无法结构化为封闭 Outcome**。keeper_guidance 承载 |
| 救猫 | `Checkpoint(rescue_cat)`。then=[ModifyOp(cat.rescued, true)] |
| 喂猫 | `Checkpoint(feed_cat)`。无检定 |
| 开门 | `Checkpoint(unlock_door)`。skills=["locksmith"]。Condition: entities.keys.door_key==true |
| 逃跑 | `Checkpoint(escape)`。skills=["DEX"] |

## Rule

| 原模组内容 | 映射 |
|-----------|------|
| 区域束缚 | `module_rules[Rule(on_scene_enter("room*"), when silver_lock.active==true, mode=forbid, then=[forbid("leave")])]` |
| 猫→NPC 因果链 | `Rule(on_scene_enter("corridor"), when cat.alive==true && cat.rescued==true, then=[trigger_rule("cat_attack_kidnapper")])` → `Rule(on_damage("kidnapper"), when..., then=[trigger_rule("kidnapper_kills_cat")])` → 最终 `Rule(on_death("kidnapper"), ..., trigger_ending("lock_broken"))` |

## WinCondition

| 原模组内容 | 映射 |
|-----------|------|
| 锁解除（终局） | `WinCondition(lock_broken, when kidnapper.alive==false, is_ending=true)` |
| 被抓回（回滚） | `Rule(on_state_change, when cat.alive==false, then=[ModifyOp(player.location, "room"), force("reset_scene")])` — 非 WinCondition |

## Visibility

| 原模组内容 | 映射 |
|-----------|------|
| 绑架者、人面鼠的秘密 | `Entity.secrets` 字段级隔离 |
| "只能看到昏暗房间" | `Scene.content` — 公开描述 |

## Gap 记录

| # | 内容 | 分类 | 说明 |
|---|------|------|------|
| 1 | 画物品（"能用平面表现构造的简单物品"） | Host Agent Gap | 无封闭 Outcome 集合。Keeper Adjudication |
| 2 | 人物卡定制 | Future Capability | 编译期个性化 |
| 3 | SAN（目击人面鼠） | Ruleset Gap | Rule + ModifyOp 可表达数值变化。骰子公式由 CoC Ruleset 解释 |
