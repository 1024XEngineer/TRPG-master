{
  "name": "trpg-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "核心流程端到端验证（issue #94）：跨 trpg-backend / trpg-sdk / trpg-frontend 三层，验证真实用户路径能否跑通",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
